#include "led_helpers.h"

Strip myStrip;
CRGB colour_healthy = CRGB(195, 195, 10);
CRGB colour_fire = CRGB(255, 20, 0);

// All your actual function implementations here
void sarasLights(Strip& strip) {
  // ... implementation
}

void regionFiresStatic(Strip& strip) {
  // ... implementation
}

// etc.